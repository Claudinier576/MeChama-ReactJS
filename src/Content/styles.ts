import styled from 'styled-components';

export const Container = styled.div`
  
  grid-area: CT;
  overflow-y: scroll;
  background-color: var(--BGContent);

  
  ::-webkit-scrollbar{
    display: none;
  }

`;

